[project]
name = "ring-phone"
version = "0.1.0"
description = "Simple SIP ring and cancel"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "nanosip",
]

[tool.uv.sources]
nanosip = { git = "https://github.com/WatskeBart/nanosip" }
